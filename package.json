{
  "name": "car-racing-season-simulator-server",
  "version": "1.0.0",
  "description": "Car racing season simulator App",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node backend/server",
    "server": "nodemon backend/server",
    "client": "npm start --prefix frontend",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install npm && run build && cd frontend && npm install npm && run build"
  },
  "engines": {
    "node": "14.19.1"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "express-async-handler": "^1.2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
